# Contributing

Issues and pull requests are welcome. For a local setup, follow the README and put your own TypeSafe key in an ignored `.env` file. Never include credentials or pasted private text in issues, tests, or pull requests.

Before opening a pull request, run:

```sh
npm run build
npm test
```

Open pull requests against `main`. Changes require one approving review and passing CI before they can merge.
