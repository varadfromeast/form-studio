# Copy to .env and replace this placeholder with your own credential.
# .env stays local and is ignored by Git.
TYPESAFE_API_KEY=replace_with_typesafe_api_key
JEV_MODEL=jev-1.13.0
PORT=3001
HOST=127.0.0.1
